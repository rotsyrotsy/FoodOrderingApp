@model FoodOrderingApp.Models.ExportInvoiceModel

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Export to PDF</title>
    <style>
        body {
            font-family: Arial, sans-serif;
        }

        .content {
            margin: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        table, th, td {
            border: 1px solid black;
        }

        th, td {
            padding: 10px;
            text-align: left;
        }
    </style>
</head>
<body>
    <div class="content">
        <h1>Order Summary of @Model.User.Email</h1>
        <p>Order ID: @Model.OrderId</p>
        <p>Order Date: @Model.Date</p>

        <p>Total Price: @Model.TotalPrice</p>

        <h2>Items</h2>
        <table>
            <thead>
                <tr>
                    <th>Item Name</th>
                    <th>Unit Price</th>
                    <th>Quantity</th>
                    <th>Amount</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var basket in Model.Baskets)
                {
                    <tr>
                        <td>@basket.DishName</td>
                        <td>@basket.Price</td>
                        <td>@basket.Quantity</td>
                        <td>@basket.BasketPrice</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</body>
</html>
